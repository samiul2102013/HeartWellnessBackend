info:
  name: 3 User Login copy
  type: http
  seq: 8

http:
  method: POST
  url: http://localhost:8000/api/auth/login/
  body:
    type: json
    data: |-
      {
        "username": "test2",
        "password": "Test123!"
      }
  auth:
    type: bearer
    token: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
